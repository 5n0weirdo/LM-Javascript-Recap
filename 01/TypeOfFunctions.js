// Performing a Task
function greet(firstName, lastName) {
    console.log("Hello" + " " + firstName + " " + lastName);
}

// Calculating a value
function sum(x, y) {
    return x + y;
}

console.log(sum(2,5));
greet("L", "M");