// [] - Array Literal - Data Structure that we use to represent a list of items
let selectedColors = ["Blue", "Red"];

selectedColors[2] = 19;

// Bracket Notation
console.log(selectedColors.length);
