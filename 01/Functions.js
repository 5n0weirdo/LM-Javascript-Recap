function greet(firstName, lastName) {
  console.log("Hello" + " " + firstName + " " + lastName);
}

function sum(x, y) {
  console.log(x + y);
}

function objects() {
  let Snoweirdo = {
    userName: "5n0weirdo",
    links: ["https://github.com/5n0weirdo", "https://ko-fi.com/snoweirdo"],
  };
  console.log(Snoweirdo.links[1]);
}

greet("LM", "Snoweirdo");
sum(3, 5);
objects()
